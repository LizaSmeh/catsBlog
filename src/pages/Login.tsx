import { Box, Typography } from "@mui/material";

export const Login = () => {
  return (
    <Box p={2}>
         <Typography variant="h4">Вход</Typography>
    <Typography variant="h6">Тут будет форма логина</Typography>
    </Box>
);
};